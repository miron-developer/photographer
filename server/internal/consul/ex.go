package consul

// ConsulRun consul test
func ConsulRun() error {
	// Get a new client

	// Get a handle to the KV API
	// kv := client.KV()

	// PUT a new KV pair
	// p := &api.KVPair{Key: "REDIS_MAXCLIENTS", Value: []byte("1200")}
	// _, err = kv.Put(p, nil)
	// if err != nil {
	// 	return err
	// }

	// Lookup the pair
	// pair, _, err := kv.Get("REDIS_MAXCLIENTS", nil)
	// if err != nil {
	// 	return err
	// }
	// fmt.Printf("KV: %v %s\n", pair.Key, pair.Value)

	return nil
}
